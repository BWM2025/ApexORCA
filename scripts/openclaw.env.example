# OpenClaw gateway env — copy this to openclaw.env in the REPO ROOT (same folder as apps/, docs/, scripts/).
# Do NOT commit openclaw.env (it's in .gitignore). Fill in your values, then: bash scripts/start-openclaw-gateway-with-env.sh
# Full reference: CREDENTIALS_AND_ENV.md (repo root). Agents: all credentials are accessible when this file is sourced before starting the gateway.

# X / Twitter (required for exec X posting)
export TWITTER_API_KEY=""
export TWITTER_API_SECRET=""
export TWITTER_ACCESS_TOKEN=""
export TWITTER_ACCESS_SECRET=""

# Email (Resend — required for exec send_email_resend)
export RESEND_API_KEY=""
export EMAIL_FROM="ApexORCA <noreply@apexorca.io>"

# Web search (Brave — for web_search tool)
export BRAVE_API_KEY=""

# LLM (e.g. Grok for pod)
export GROK_API_KEY=""

# Deploy
export VERCEL_TOKEN=""

# Optional: Vector / RAG
export PINECONE_API_KEY=""
export PINECONE_ENV=""

# Optional: Proxy (Bright Data)
export BRIGHT_DATA_API_KEY=""
export BRIGHT_DATA_HOST=""
export BRIGHT_DATA_PORT=""
export BRIGHT_DATA_USER=""
export BRIGHT_DATA_PASS=""

# Optional: Sentry (Next.js can use in apps/web/.env.local)
export NEXT_PUBLIC_SENTRY_DSN=""

# Optional: Stripe (if scripts need; web app uses apps/web/.env)
export STRIPE_PUBLISHABLE_KEY=""
export STRIPE_SECRET_KEY=""

# Optional: Discord bot
export DISCORD_APPLICATION_ID=""
export DISCORD_PUBLIC_KEY=""
export DISCORD_BOT_TOKEN=""

# Optional: Telegram bot
export TELEGRAM_BOT_TOKEN=""
